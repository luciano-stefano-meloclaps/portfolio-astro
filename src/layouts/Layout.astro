---
// src/layouts/Layout.astro
import { SEO } from 'astro-seo';
import '@/styles/global.css';

import ThemeManager from '@/components/modules/ThemeManager.astro';
import Navbar from '@/components/modules/Navbar.astro';
import MobileMenu from '@/components/modules/MobileMenu.astro';

export interface Props {
  title?: string;
  description?: string;
  image?: string;
  canonicalURL?: string;
  // allow preloading additional images for LCP optimization
  preloadImages?: string[];
  // Propiedad arquitectónica dinámica para sincronizar el LCP del originador con el Head
  preloadResponsiveImage?: {
    srcSet: string;
    sizes: string;
    type?: string;
  };
}

const fullName = 'Luciano Stefano Melo Claps';
const defaultTitle = `${fullName} | Software Developer`;
const defaultDescription = `${fullName} | Desarrollador Fullstack .NET & React. Soluciones de software escalables, UX/UI y optimización técnica. Conocé mi trayectoria.`;
const siteUrl = 'https://meloclaps.com';

const {
  title = defaultTitle,
  description = defaultDescription,
  canonicalURL = Astro.url.href,
  preloadImages = [],
  preloadResponsiveImage,
} = Astro.props;

import { getImage } from 'astro:assets';
import ogImageBase from '@/assets/images/og-image.jpg';

// Procesamiento Dinámico de la Imagen OG (Garantiza 1200x630 y URLs absolutas)
const optimizedOgImage = await getImage({
  src: ogImageBase,
  width: 1200,
  height: 630,
  format: 'jpg',
  quality: 90
});
const ogImageUrl = new URL(optimizedOgImage.src, siteUrl).href;

// build breadcrumb items based on current path
const pathname = Astro.url.pathname; // e.g. '/certifications/aws-cloud-foundations/'
const segments = pathname.split('/').filter((s) => s !== '');

function segmentName(seg: string): string {
  // map known slugs to readable names
  const map: Record<string, string> = {
    certifications: 'Certificaciones',
    projects: 'Proyectos',
  };
  return map[seg] || seg.replace(/-/g, ' ');
}

const breadcrumbItems = [
  {
    '@type': 'ListItem',
    position: 1,
    name: 'Inicio',
    item: siteUrl,
  },
];

segments.forEach((seg, idx) => {
  const position = breadcrumbItems.length + 1;
  breadcrumbItems.push({
    '@type': 'ListItem',
    position,
    name: segmentName(seg),
    item: new URL(seg + '/', siteUrl).href,
  });
});

const personSchema = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: fullName,
  url: siteUrl,
  image: ogImageUrl,
  sameAs: [
    'https://github.com/luciano-stefano-meloclaps',
    'https://linkedin.com/in/luciano-stefano-melo-claps',
  ],
  jobTitle: 'Software Developer',
  knowsAbout: ['.NET', 'React', 'Astro', 'TypeScript', 'UX/UI Design', 'SEO'],
  description: defaultDescription,
};
---

<!doctype html>
<html lang="es" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, viewport-fit=cover"
    />
    <meta name="generator" content={Astro.generator} />
    <meta name="theme-color" content="#000000" />

    <link rel="canonical" href={canonicalURL} />
    
    {/* Favicons (explicit for Google Search) */}
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="icon" type="image/png" href="/favicon.png" sizes="32x32" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" href="/favicon.png" />

    {/* preconnect for potential font/CDN usage */}
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    
    {/* Precarga Dinámica Asincrónica para LCP Mobile */}
    {
      preloadResponsiveImage && (
        <link 
          rel="preload" 
          as="image" 
          imagesrcset={preloadResponsiveImage.srcSet} 
          imagesizes={preloadResponsiveImage.sizes}
          type={preloadResponsiveImage.type || 'image/webp'} 
        />
      )
    }

    {
      preloadImages.map((url: string) => (
        <link rel="preload" as="image" href={url} />
      ))
    }

    <SEO
      title={title}
      description={description}
      canonical={canonicalURL}
      openGraph={{
        basic: { title, type: 'website', image: ogImageUrl, url: canonicalURL },
        image: { 
          alt: `${fullName} - Portfolio`, 
          width: optimizedOgImage.attributes.width, 
          height: optimizedOgImage.attributes.height,
          type: 'image/jpeg' 
        },
        optional: { siteName: `${fullName} Portfolio`, locale: 'es_AR' },
      }}
      twitter={{
        card: 'summary_large_image',
        title,
        description,
        image: ogImageUrl,
      }}
      extend={{
        meta: [
          { name: 'robots', content: 'index, follow' },
          { name: 'googlebot', content: 'index, follow' },
          { name: 'thumbnail', content: ogImageUrl },
        ],
      }}
    />

    <ThemeManager />

    {/* Structured data: person + website */}
    <script
      type="application/ld+json"
      set:html={JSON.stringify(personSchema)}
    />
    <script
      type="application/ld+json"
      set:html={JSON.stringify({
        '@context': 'https://schema.org',
        '@type': 'WebSite',
        url: siteUrl,
        name: fullName,
        image: ogImageUrl,
        potentialAction: {
          '@type': 'SearchAction',
          target: `${siteUrl}?s={search_term_string}`,
          'query-input': 'required name=search_term_string',
        },
      })}
    />

    {/* optional breadcrumb list for richer snippets */}
    {
      segments.length > 0 && (
        <script
          type="application/ld+json"
          set:html={JSON.stringify({
            '@context': 'https://schema.org',
            '@type': 'BreadcrumbList',
            itemListElement: breadcrumbItems,
          })}
        />
      )
    }
  </head>

  <body
    class="selection:bg-accent-magenta/30 overflow-x-hidden bg-background font-sans text-text-primary antialiased"
  >
    <div
      class="pointer-events-none fixed inset-0 z-[-1]"
      style="background-size: 4.5rem 4.5rem; background-image: linear-gradient(to right, rgb(var(--color-grid-line) / 0.05) 1px, transparent 1px), linear-gradient(to bottom, rgb(var(--color-grid-line) / 0.05) 1px, transparent 1px);"
      aria-hidden="true"
    >
    </div>
    <Navbar />
    <MobileMenu />
    <slot />
  </body>
</html>
